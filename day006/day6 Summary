6일차
생성자 멤버변수 defult 사용법과 인자가 있는 생성자 호출법 복습
오버라이딩으로 종속되는 클래스 사용
추상화를 통해 추상클래스 선언과 오버라이딩으로 이에 종속 시키는 과정
얕은 복사 (Shallow Copy): 얕은 복사는 객체의 필드 값들을 복사할 때, 참조 형식의 필드는 참조(주소)만 복사하는 것을 말합니다. 
따라서 원본 객체와 복사된 객체는 같은 객체를 참조하고 있으며, 한쪽에서 필드 값을 변경하면 다른쪽에서도 영향을 받습니다.
 즉, 복사된 객체는 원본 객체와 동일한 참조를 가지고 있기 때문에 변경이 공유됩니다.
Myclass target = source;

깊은 복사 (Deep Copy): 깊은 복사는 객체의 필드 값들을 복사할 때, 참조 형식의 필드도 독립적으로 복사하는 것을 말합니다. 
필드 값들이 복사될 때 새로운 객체가 생성되며, 원본 객체와 복사된 객체는 완전히 독립적으로 동작합니다. 
따라서 한쪽에서 필드 값을 변경해도 다른쪽에는 영향을 주지 않습니다.
공간을 새로 만들어서 넣는다
Myclass target = source.DeepCopy
