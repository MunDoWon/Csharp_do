7일차 OOPA14~prime Quiz

is-a관계  	상속관계
has-a관계  종속(포함) 관계
interface 다중클래스 상속이 안되므로 클래스를 사용함 클래스는 구현메소드가 없기에 구현할때 반드시 재정의 해줘야함
this.logger = logger; 대상객체에서 구현할때 받은 매개변수가 사라지지 않게 복사해야함
ClimateMonitor monitor = new ClimateMonitor(new FileLogger("My Log.txt"));
파일로그를 작성
init 읽기전용 초기화 자로 set 대신 사용
var a = new { Name = "홍길동", Age = 123 };     //var에서 문자열과 정수를 덧셈으로 붙이면 둘다 문자가 됨
 Console.WriteLine(a.Name+ " " + a.Age);
